import 'package:flutter/material.dart';

class AcademicNote extends StatelessWidget {
  const AcademicNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Academic Note"),
      ),
    );
  }
}