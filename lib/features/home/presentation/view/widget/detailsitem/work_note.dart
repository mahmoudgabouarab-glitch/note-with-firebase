import 'package:flutter/material.dart';

class WorkNote extends StatelessWidget {
  const WorkNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Work Note")));
  }
}
