import 'package:flutter/material.dart';

class BusinessNote extends StatelessWidget {
  const BusinessNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Business Note"),
      ),
    );
  }
}