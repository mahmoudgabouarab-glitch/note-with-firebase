import 'package:flutter/material.dart';

class FamilyNote extends StatelessWidget {
  const FamilyNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Family Note"),
      ),
    );
  }
}