import 'package:flutter/material.dart';

class OthersNote extends StatelessWidget {
  const OthersNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Others Note"),
      ),
    );
  }
}